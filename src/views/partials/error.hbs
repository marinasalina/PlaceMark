<!-- Display validation or server error if any exist -->
{{#if errors}}
  <div class="box content">
    <p> There was a problem... </p>
    <!-- List each error message -->
    <ul>
      {{#each errors}}
        <li>{{message}}</li>
      {{/each}}
    </ul>
  </div>
{{/if}}